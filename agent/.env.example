# OpenCode server — GreenNode MaaS provider (OpenAI-compatible)
GREENNODE_BASE_URL=https://maas.greennode.ai/v1
GREENNODE_API_KEY=replace-me

# FDL MCP server (governed query, per-user masking). Parked in the current
# build — point this at the real FDL MCP endpoint when the data layer lands.
FDL_MCP_URL=http://localhost:7070/mcp
