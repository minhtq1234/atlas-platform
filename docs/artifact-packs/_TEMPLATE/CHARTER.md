# <Type> Pack — Charter (Phase 0)

**Type:** `<Type>` · **Status:** chartered <date> · module at `services/bff/src/artifacts/<type>/`

## Why this pack / what's weak today
<One paragraph: what the type produces now and where it falls short.>

## Target archetypes (2–4)
- **<Archetype A>** — <audience; purpose>.
- **<Archetype B>** — <audience; purpose>.
<List deferred archetypes.>

## Audience & context
<Who reads these; any sovereignty/tone constraints.>

## Definition of done
- Anchor archetypes pass `RUBRIC.md` on the full `EVAL-PROMPTS.md` set.
- <Type-specific must-haves> supported end-to-end (schema → renderer → export).
- `guidance(archetypeId)` + `archetypes` array populated.
- ≥1 committed synthetic seed per archetype; real exemplars de-sensitized + logged.
- All three suites green; export opens clean.

## Scope guardrail (YAGNI)
In: <the focused set>. Out (this cycle): <explicitly deferred>.

## Build scope, ranked (feeds the build plan)
- **P0** — <biggest levers; usually prompt guidance + archetypes + one schema add>.
- **P1** — <next>.
- **P2 / deferred** — <later>.
